name: Move new issues into group-j
on:
  issues:
    types: [opened]

jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.3.0
        with:
          project: group-j
          column: To do
          repo-token: ${{ secrets.GH_TOKEN }}
